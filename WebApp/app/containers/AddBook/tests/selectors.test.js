// import { fromJS } from 'immutable';
// import { selectAddBookDomain } from '../selectors';

describe('selectAddBookDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
